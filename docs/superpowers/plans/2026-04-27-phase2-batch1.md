# Phase 2 Batch 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade prompt quality with bbox-based char estimation and style presets; add template consistency checks and prompt retry.

**Architecture:** Four independent features touching a shared schema layer. Build order: schema first → estimation algo → style files + prompt builder → state_manager consistency check → UI integration for all features at once.

**Tech Stack:** Python 3.12, Pydantic v2, Jinja2, Streamlit, python-pptx, pytest

---

## File Map

| Action | File | Responsibility |
|--------|------|---------------|
| Modify | `config.yaml` | Add `default_style: "huawei"` |
| Modify | `src/schema.py` | Add fields: ShapeRole.min_chars, TemplateMeta.layout_type, TaskRun.style_id/revision_count/revision_notes, AppConfig.default_style |
| Modify | `src/core/role_inferencer.py` | Add `estimate_char_capacity()`, update `infer_roles()` |
| Create | `prompts/styles/huawei.md` | Huawei text-generation style preset |
| Create | `prompts/styles/minimal.md` | Minimal style placeholder |
| Modify | `prompts/text_generation.md.j2` | Add style block, layout_type block, update char display |
| Create | `prompts/text_revision.md.j2` | Revision prompt template |
| Modify | `src/prompt/text_prompt_builder.py` | Load style file, pass style_content/layout_type to template |
| Modify | `src/utils/state_manager.py` | Add `check_template_consistency()` |
| Modify | `src/ui/pages/1_template_library.py` | Add layout_type dropdown |
| Modify | `src/ui/pages/2_new_task.py` | Style dropdown, consistency check, retry section |
| Create | `tests/test_char_estimation.py` | Tests for bbox char estimation |
| Modify | `tests/test_pptx_parser.py` | Verify min_chars populated after infer_roles |

---

### Task 1: Schema Changes

**Files:**
- Modify: `src/schema.py:55-88` (ShapeRole), `src/schema.py:90-112` (TemplateMeta), `src/schema.py:120-142` (TaskRun), `src/schema.py:145-153` (AppConfig)
- Modify: `config.yaml`

- [ ] **Step 1: Add min_chars to ShapeRole**

In `src/schema.py`, add `min_chars` field after `max_chars` (line 70):

```python
    max_chars: Optional[int] = None
    min_chars: Optional[int] = None
```

- [ ] **Step 2: Add layout_type to TemplateMeta**

In `src/schema.py`, add field after `slide_height_emu` (line 99):

```python
    slide_height_emu: int = 0

    layout_type: Optional[str] = None
```

- [ ] **Step 3: Add style_id and revision fields to TaskRun**

In `src/schema.py`, add fields after `template_mtime` (line 127):

```python
    template_mtime: float = 0.0

    style_id: Optional[str] = None
    revision_count: int = 0
    revision_notes: Optional[str] = None
```

- [ ] **Step 4: Add default_style to AppConfig**

In `src/schema.py`, add field after `candidates_per_element` (line 153):

```python
    candidates_per_element: int = 3
    default_style: str = "huawei"
```

- [ ] **Step 5: Update config.yaml**

Add at end of `config.yaml`:

```yaml
default_style: "huawei"
```

- [ ] **Step 6: Run existing tests to verify no regressions**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/ -v`
Expected: All existing tests pass (new optional fields have defaults, so no breakage).

- [ ] **Step 7: Commit**

```bash
git add src/schema.py config.yaml
git commit -m "feat: add schema fields for phase2 — min_chars, layout_type, style_id, revision support"
```

---

### Task 2: BBox-Based Character Count Estimation

**Files:**
- Modify: `src/core/role_inferencer.py`
- Create: `tests/test_char_estimation.py`

- [ ] **Step 1: Write the failing test**

Create `tests/test_char_estimation.py`:

```python
"""Tests for bbox-based character count estimation."""

from src.schema import BBox, FontInfo, ShapeRole, ShapeType, TextSubtype
from src.core.role_inferencer import estimate_char_capacity, infer_roles


def test_estimate_basic():
    """A 200pt x 100pt box at 12pt should hold ~9 chars/line × ~5 lines."""
    bbox = BBox(left=0, top=0, width=200 * 12700, height=100 * 12700)
    min_c, max_c = estimate_char_capacity(bbox, font_size_pt=12.0)
    assert max_c > min_c > 0
    assert 40 <= min_c <= 60
    assert 60 <= max_c <= 90


def test_estimate_title_single_line():
    """Title should be capped to single line."""
    bbox = BBox(left=0, top=0, width=400 * 12700, height=50 * 12700)
    min_c, max_c = estimate_char_capacity(bbox, font_size_pt=24.0, is_title=True)
    assert max_c <= 20


def test_estimate_tiny_box():
    """Very small box should return at least min_chars=1."""
    bbox = BBox(left=0, top=0, width=20 * 12700, height=20 * 12700)
    min_c, max_c = estimate_char_capacity(bbox, font_size_pt=12.0)
    assert min_c >= 1
    assert max_c >= min_c


def test_estimate_large_font():
    """Large font means fewer chars."""
    bbox = BBox(left=0, top=0, width=300 * 12700, height=60 * 12700)
    min_small, max_small = estimate_char_capacity(bbox, font_size_pt=10.0)
    min_large, max_large = estimate_char_capacity(bbox, font_size_pt=24.0)
    assert max_small > max_large


def test_infer_roles_uses_bbox_estimation():
    """infer_roles should populate min_chars from bbox, not 2x heuristic."""
    elements = [
        ShapeRole(
            shape_id=1,
            shape_name_original="Title 1",
            type=ShapeType.TEXT,
            bbox=BBox(left=0, top=0, width=400 * 12700, height=50 * 12700),
            first_run_font=FontInfo(size_pt=24.0),
            current_content="Short",
            paragraph_count=1,
        ),
    ]
    slide_w = 9144000
    slide_h = 6858000
    result = infer_roles(elements, slide_w, slide_h)
    el = result[0]
    assert el.min_chars is not None
    assert el.min_chars > 0
    assert el.max_chars != max(len("Short") * 2, 50)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/test_char_estimation.py -v`
Expected: FAIL — `estimate_char_capacity` not found.

- [ ] **Step 3: Implement estimate_char_capacity and update infer_roles**

Replace the entire content of `src/core/role_inferencer.py`:

```python
"""Rule-based role inference for shape elements."""

from __future__ import annotations

from statistics import median

from src.schema import BBox, ShapeRole, ShapeType, TextSubtype

EMU_PER_PT = 12700


def estimate_char_capacity(
    bbox: BBox,
    font_size_pt: float,
    is_title: bool = False,
) -> tuple[int, int]:
    """Estimate (min_chars, max_chars) a text box can hold based on physical dimensions."""
    bbox_width_pt = bbox.width / EMU_PER_PT
    bbox_height_pt = bbox.height / EMU_PER_PT

    if font_size_pt <= 0:
        font_size_pt = 12.0

    chars_per_line = bbox_width_pt / font_size_pt
    line_height = font_size_pt * 1.5
    lines = bbox_height_pt / line_height

    if is_title:
        lines = 1.0

    theoretical_max = chars_per_line * lines
    max_chars = max(int(theoretical_max * 0.85), 1)
    min_chars = max(int(theoretical_max * 0.55), 1)

    return (min_chars, max_chars)


def _resolve_font_size(el: ShapeRole, fallback_pt: float) -> float:
    """Get font size: element's own font > fallback median > 12pt."""
    if el.first_run_font and el.first_run_font.size_pt:
        return el.first_run_font.size_pt
    return fallback_pt


def infer_roles(
    elements: list[ShapeRole],
    slide_w_emu: int,
    slide_h_emu: int,
) -> list[ShapeRole]:
    title_counter = 0
    body_counter = 0
    image_counter = 0

    body_font_sizes = []
    for el in elements:
        if el.type == ShapeType.TEXT and el.first_run_font and el.first_run_font.size_pt:
            if el.first_run_font.size_pt < 24:
                body_font_sizes.append(el.first_run_font.size_pt)
    fallback_font_pt = median(body_font_sizes) if body_font_sizes else 12.0

    for el in elements:
        if not el.is_editable:
            continue

        if el.type == ShapeType.IMAGE:
            image_counter += 1
            el.role_key = f"image_{image_counter:02d}"
            el.role_zh = f"图片{image_counter}"
            el.role_confirmed = False
            continue

        if el.type != ShapeType.TEXT:
            continue

        font_size = el.first_run_font.size_pt if el.first_run_font else None
        area = el.bbox.width * el.bbox.height
        slide_area = slide_w_emu * slide_h_emu
        area_ratio = area / slide_area if slide_area > 0 else 0

        if area_ratio < 0.02:
            el.type = ShapeType.DECORATION
            el.is_editable = False
            continue

        in_top_quarter = el.bbox.top < slide_h_emu * 0.25

        if font_size and font_size >= 24 and in_top_quarter:
            title_counter += 1
            el.role_key = "title_main" if title_counter == 1 else f"title_{title_counter:02d}"
            el.role_zh = "主标题" if title_counter == 1 else f"标题{title_counter}"
            el.text_subtype = TextSubtype.TITLE
        elif el.paragraph_count and el.paragraph_count > 1:
            body_counter += 1
            el.role_key = f"bullet_{body_counter:02d}"
            el.role_zh = f"要点{body_counter}"
            el.text_subtype = TextSubtype.BULLET
        else:
            body_counter += 1
            el.role_key = f"body_{body_counter:02d}"
            el.role_zh = f"正文{body_counter}"
            el.text_subtype = TextSubtype.BODY

        el.role_confirmed = False

        is_title = el.text_subtype == TextSubtype.TITLE
        resolved_font = _resolve_font_size(el, fallback_font_pt)
        el.min_chars, el.max_chars = estimate_char_capacity(el.bbox, resolved_font, is_title=is_title)
        el.max_lines = el.paragraph_count or 1

    return elements
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/test_char_estimation.py -v`
Expected: All 5 tests PASS.

- [ ] **Step 5: Run all tests for regressions**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/ -v`
Expected: All tests pass.

- [ ] **Step 6: Commit**

```bash
git add src/core/role_inferencer.py tests/test_char_estimation.py
git commit -m "feat: bbox-based char estimation replacing 2x heuristic"
```

---

### Task 3: Style Preset Files

**Files:**
- Create: `prompts/styles/huawei.md`
- Create: `prompts/styles/minimal.md`

- [ ] **Step 1: Create prompts/styles/ directory**

```bash
mkdir -p C:\Users\lintian\ppt-assistant\prompts\styles
```

- [ ] **Step 2: Create huawei.md**

Create `prompts/styles/huawei.md`:

```markdown
# 华为式技术汇报 PPT 文案风格

## 标题规范
- 必须是观点型/结论型标题，禁止纯名词主题（如"模型优化""方案设计"）
- 标题公式：「对象 + 冒号 + 方法/动作 + 结果/收益」
- 标题中直接嵌入量化数据，如 +20%、1.2X、35%→60%
- 示例：
  · "L1数据突破总体进展：现网数据增训提升泛化性+信道图谱场景性能提高，L1模型性能+20%"
  · "处理流程和数据关键技术，使能高价值数据占比35%→60%"
  · "优化图谱架构，拓展图谱范围，增加图谱应用，仿真兑现图谱增益18%"

## 标题句式模板
- 进展类："XX进展：优化XX架构，拓展XX范围，XX增益达XX%"
- 突破类："XX突破：通过XX技术增强，实现XX性能+XX%"
- 价值类："XX价值：基于XX特征建模，增强XX能力"
- 对比类："XX对比：从As-Is到To-be，构建XX闭环"
- 全景类："XX技术全景：用户级→小区级→网络级全维度应用，XX性能1.2X"

## 语言措辞风格
- 高频动词：构建、使能、提升、增强、优化、拓展、演进、覆盖、新增、沉淀、解耦、闭环
- 高频名词：架构、底座、场景、增益、机制、能力、链路、模型、特征、维度
- 常用句式：
  · "面向XX场景，构建XX能力"
  · "基于XX数据/特征/模型，增强XX能力"
  · "相比AI 2.0/现网/传统方案，XX提升XX%"
  · "通过XX实现XX，收益达XX"
  · "从XX到XX的演进路径"
- 英文缩写可直接使用（MSG、RAG、RRM、PAPR等），无需翻译

## Bullet 规范
- 短句为主，术语化、结论化
- 每条 Bullet 表达一个完整的技术论点或数据支撑
- 结构：动作/方法 + 效果/收益
- 避免泛泛描述，必须有具体技术细节或数据

## 反向约束（绝对禁止）
- ✗ 强营销感标语（"颠覆""赋能未来""引领行业"等空话）
- ✗ 描述型纯名词标题（如"模型优化""方案设计"）
- ✗ 大段叙述文字 / 逻辑松散 / 重点不清
- ✗ 口语化、情绪化表达
- ✗ 一页多个主结论（每页只表达一个核心主旨）
```

- [ ] **Step 3: Create minimal.md**

Create `prompts/styles/minimal.md`:

```markdown
# 简约清晰风格

## 标题规范
- 简洁直达，一句话概括核心观点
- 可用名词短语或动宾短语
- 避免冗长修饰

## 语言风格
- 精炼、克制、平实
- 避免堆叠形容词
- 一句话一个信息点

## Bullet 规范
- 每条不超过 20 字
- 关键词前置
- 并列结构统一

## 反向约束
- ✗ 华丽辞藻
- ✗ 过度修饰
- ✗ 信息重复
```

- [ ] **Step 4: Commit**

```bash
git add prompts/styles/
git commit -m "feat: add style presets — huawei and minimal"
```

---

### Task 4: Prompt Template Upgrades

**Files:**
- Modify: `prompts/text_generation.md.j2`
- Create: `prompts/text_revision.md.j2`
- Modify: `src/prompt/text_prompt_builder.py`

- [ ] **Step 1: Update text_generation.md.j2**

Replace entire content of `prompts/text_generation.md.j2`:

```jinja2
你的任务：{{ task_description }}
{% if style_content %}

═══ 写作风格要求 ═══
{{ style_content }}
{% endif %}
{% if layout_type %}

═══ 当前版式类型 ═══
{% if layout_type == "dual_column" %}本页为双栏并列版式，适合 As-Is vs To-be、用户级 vs 网络级等对比分析。请生成对比式内容。
{% elif layout_type == "top_summary" %}本页为上总下分版式，上方放总结论/总流程，下方拆模块展开。请先写总结论，再写分模块要点。
{% elif layout_type == "flow_chain" %}本页为流程链版式，中间主链路，周围输入/增强/应用/收益。请按流程步骤组织内容。
{% elif layout_type == "card_grid" %}本页为多模块卡片版式，每模块独立。请为每个模块生成独立完整的内容。
{% else %}版式类型：{{ layout_type }}。请根据版式特点组织内容结构。
{% endif %}
{% endif %}

请为以下 PPT 元素各生成 {{ n_candidates }} 个备选文案，严格按 JSON 格式输出。

元素列表：
{% for e in elements %}
- "{{ e.role_key }}": {{ e.role_zh }}{% if e.text_subtype %}（{{ e.text_subtype.value }}类型）{% endif %}{% if e.min_chars and e.max_chars %}，建议{{ e.min_chars }}-{{ e.max_chars }}字，尽量充实内容不留空白{% elif e.max_chars %}，最多{{ e.max_chars }}字{% endif %}{% if e.text_subtype and e.text_subtype.value == "title" %}，控制在一行内{% endif %}

  原文参考：{{ e.current_content or "无" }}
{% endfor %}

输出格式要求：
1. 严格 JSON，不要有注释或额外解释
2. 每个 key 对应一个列表，列表内 {{ n_candidates }} 个字符串

```json
{
{% for e in elements %}  "{{ e.role_key }}": ["备选A", "备选B", "备选C"]{% if not loop.last %},{% endif %}

{% endfor %}}
```
```

- [ ] **Step 2: Create text_revision.md.j2**

Create `prompts/text_revision.md.j2`:

```jinja2
以下是你上次为 PPT 生成的文案（需要修改）：

```json
{{ previous_response }}
```

修改要求：
{{ revision_notes }}

请基于以上要求重新生成全部文案，输出格式与上次完全一致（严格 JSON，每个 key 对应 {{ n_candidates }} 个备选字符串）。
```

- [ ] **Step 3: Update text_prompt_builder.py**

Replace entire content of `src/prompt/text_prompt_builder.py`:

```python
"""Generate text prompts from template metadata using Jinja2."""

from __future__ import annotations

from pathlib import Path

from jinja2 import Environment, FileSystemLoader

from src.schema import TemplateMeta
from src.utils.config import resolve_path, load_config


def _load_style_content(style_id: str | None) -> str | None:
    """Read a style preset file from prompts/styles/{style_id}.md. Returns None if not found."""
    if not style_id:
        return None
    config = load_config()
    style_path = resolve_path(config.prompts_dir) / "styles" / f"{style_id}.md"
    if not style_path.exists():
        return None
    return style_path.read_text(encoding="utf-8")


def list_available_styles() -> list[str]:
    """Return list of style IDs (filenames without .md) from prompts/styles/."""
    config = load_config()
    styles_dir = resolve_path(config.prompts_dir) / "styles"
    if not styles_dir.exists():
        return []
    return sorted(p.stem for p in styles_dir.glob("*.md"))


def build_text_prompt(
    meta: TemplateMeta,
    task_description: str,
    n_candidates: int = 3,
    style_id: str | None = None,
) -> str:
    config = load_config()
    prompts_dir = resolve_path(config.prompts_dir)

    env = Environment(
        loader=FileSystemLoader(str(prompts_dir)),
        keep_trailing_newline=True,
    )
    template = env.get_template("text_generation.md.j2")

    elements = meta.editable_text_elements
    elements_with_roles = [e for e in elements if e.role_key]

    style_content = _load_style_content(style_id)

    return template.render(
        task_description=task_description,
        n_candidates=n_candidates,
        elements=elements_with_roles,
        style_content=style_content,
        layout_type=meta.layout_type,
    )


def build_revision_prompt(
    previous_response: str,
    revision_notes: str,
    n_candidates: int = 3,
) -> str:
    config = load_config()
    prompts_dir = resolve_path(config.prompts_dir)

    env = Environment(
        loader=FileSystemLoader(str(prompts_dir)),
        keep_trailing_newline=True,
    )
    template = env.get_template("text_revision.md.j2")

    return template.render(
        previous_response=previous_response,
        revision_notes=revision_notes,
        n_candidates=n_candidates,
    )
```

- [ ] **Step 4: Run all tests**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/ -v`
Expected: All tests pass.

- [ ] **Step 5: Commit**

```bash
git add prompts/text_generation.md.j2 prompts/text_revision.md.j2 src/prompt/text_prompt_builder.py
git commit -m "feat: upgrade prompt templates with style injection, layout hints, char ranges, and revision support"
```

---

### Task 5: Template Consistency Check

**Files:**
- Modify: `src/utils/state_manager.py`

- [ ] **Step 1: Add check_template_consistency to state_manager.py**

Append to end of `src/utils/state_manager.py` (after `list_template_metas`):

```python
def check_template_consistency(meta: TemplateMeta) -> bool:
    """Return True if template file mtime matches metadata snapshot. False if stale or file missing."""
    template_path = Path(meta.file_path)
    if not template_path.exists():
        return False
    return abs(template_path.stat().st_mtime - meta.file_mtime) < 0.01
```

- [ ] **Step 2: Run all tests**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/ -v`
Expected: All pass.

- [ ] **Step 3: Commit**

```bash
git add src/utils/state_manager.py
git commit -m "feat: add template consistency check via mtime comparison"
```

---

### Task 6: UI — Template Library layout_type Dropdown

**Files:**
- Modify: `src/ui/pages/1_template_library.py`

- [ ] **Step 1: Add LAYOUT_OPTIONS constant and dropdown**

In `src/ui/pages/1_template_library.py`, add the layout options constant after `SUBTYPE_OPTIONS` (line 89):

```python
SUBTYPE_OPTIONS = ["", "title", "bullet", "body"]
LAYOUT_OPTIONS = ["", "dual_column", "top_summary", "flow_chain", "card_grid"]
```

- [ ] **Step 2: Add layout_type selector above the per-element loop**

After the annotated image display (after line 82), before the `ROLE_OPTIONS` definition, add:

```python
st.markdown("**版式类型**")
current_layout = meta.layout_type or ""
layout_labels = {
    "": "未设置",
    "dual_column": "双栏并列（对比分析）",
    "top_summary": "上总下分",
    "flow_chain": "流程链",
    "card_grid": "多模块卡片",
}
new_layout = st.selectbox(
    "版式类型",
    LAYOUT_OPTIONS,
    index=LAYOUT_OPTIONS.index(current_layout) if current_layout in LAYOUT_OPTIONS else 0,
    format_func=lambda x: layout_labels.get(x, x),
    key="layout_type_select",
)
if new_layout != current_layout:
    meta.layout_type = new_layout or None
    changed = True
```

- [ ] **Step 3: Verify manually**

Run: `cd C:\Users\lintian\ppt-assistant && python -m streamlit run src/ui/pages/1_template_library.py`
Verify: The "角色修正" section shows a "版式类型" dropdown above element list.

- [ ] **Step 4: Commit**

```bash
git add src/ui/pages/1_template_library.py
git commit -m "feat: add layout_type dropdown in template library"
```

---

### Task 7: UI — New Task Page (Style, Consistency, Retry)

**Files:**
- Modify: `src/ui/pages/2_new_task.py`

This is the largest task — adds style dropdown in Step 2, consistency check in Step 1/5, and retry section in Step 4.

- [ ] **Step 1: Update imports**

Replace the import block at top of `src/ui/pages/2_new_task.py`:

```python
"""New task workflow: select template -> generate prompt -> paste JSON -> preview 3 candidates -> export."""

from datetime import datetime
from pathlib import Path

import streamlit as st

from src.utils.config import load_config, resolve_path
from src.utils.state_manager import (
    list_template_metas, load_template_meta, save_task, save_raw_response,
    check_template_consistency,
)
from src.prompt.text_prompt_builder import build_text_prompt, build_revision_prompt, list_available_styles
from src.prompt.json_parser import parse_ai_json
from src.core.pptx_filler import fill_template
from src.core.pptx_renderer import render_slide_to_png, render_slides_to_png
from src.schema import TaskRun, TextCandidates

try:
    from st_copy_to_clipboard import st_copy_to_clipboard
except ImportError:
    st_copy_to_clipboard = None
```

- [ ] **Step 2: Add consistency check after template selection (Step 1)**

Replace the Step 1 section (lines 33-40) with:

```python
# --- Step 1: Select template ---
st.subheader("Step 1: 选择模板")
template_ids = [m.template_id for m in all_metas]
selected_id = st.selectbox("模板", template_ids)
meta = load_template_meta(selected_id)

if meta:
    if not check_template_consistency(meta):
        st.warning("⚠ 模板文件已被修改或不存在，建议重新预处理。")
        c1, c2 = st.columns(2)
        with c1:
            if st.button("重新预处理", key="repreprocess_step1"):
                st.switch_page("pages/1_template_library.py")
        with c2:
            if st.button("忽略继续", key="ignore_consistency_step1"):
                pass

    if meta.annotated_image and Path(meta.annotated_image).exists():
        st.image(meta.annotated_image, use_container_width=True)
```

- [ ] **Step 3: Add style dropdown in Step 2**

Replace the Step 2 section (lines 43-47) with:

```python
# --- Step 2: 任务描述 & 风格选择 ---
st.subheader("Step 2: 任务描述 & 风格")
task_desc = st.text_area("描述你的 PPT 主题", placeholder="例如：介绍最新的 L2O 优化算法")

available_styles = list_available_styles()
style_options = ["(无)"] + available_styles
default_idx = style_options.index(config.default_style) if config.default_style in style_options else 0
selected_style = st.selectbox("写作风格", style_options, index=default_idx)
style_id = None if selected_style == "(无)" else selected_style

if not task_desc:
    st.stop()
```

- [ ] **Step 4: Update task initialization to include style_id**

Replace the task initialization block (lines 50-58) with:

```python
# --- Initialize task ---
if "current_task" not in st.session_state:
    now = datetime.now()
    task_id = f"{now.strftime('%Y-%m-%d')}_{task_desc[:20].replace(' ', '_')}"
    st.session_state["current_task"] = TaskRun(
        task_id=task_id,
        task_description=task_desc,
        template_id=selected_id,
        template_mtime=meta.file_mtime if meta else 0,
        style_id=style_id,
    )

task: TaskRun = st.session_state["current_task"]
task.style_id = style_id
```

- [ ] **Step 5: Update prompt generation to pass style_id**

Replace the Step 3 prompt generation block (lines 63-74) with:

```python
# --- Step 3: Generate prompt ---
st.subheader("Step 3: 生成 Prompt → 复制到 AI Web")
if meta:
    prompt = build_text_prompt(meta, task_desc, config.candidates_per_element, style_id=style_id)
    task.text_prompt = prompt
    task.status = "prompting"
    task.current_step = 3

    st.code(prompt, language="markdown")
    if st_copy_to_clipboard:
        st_copy_to_clipboard(prompt, "复制 Prompt")
    else:
        st.info("安装 st-copy-to-clipboard 后可一键复制。手动选中上方文本复制。")
```

- [ ] **Step 6: Add retry section after Step 4 JSON paste**

Replace the Step 4 section (lines 77-97) with:

```python
# --- Step 4: Paste AI JSON ---
st.subheader("Step 4: 粘贴 AI 返回的 JSON")
raw_json = st.text_area("粘贴 AI 返回结果", height=200, key="raw_json_input")

if not raw_json:
    st.stop()

parsed = parse_ai_json(raw_json)
if parsed is None:
    st.error("无法解析 JSON。请检查格式后重试。原文已保存。")
    task.ai_response_raw = raw_json
    save_raw_response(task.task_id, raw_json)
    st.stop()

task.ai_response_raw = raw_json
task.text_candidates = TextCandidates(
    template_id=selected_id,
    task_description=task_desc,
    candidates=parsed,
)
task.status = "selecting"
task.current_step = 5

# --- Retry section ---
with st.expander("对结果不满意？重新生成"):
    QUICK_NOTES = [
        "内容太短，每个文本框至少写满建议字数范围",
        "标题不够结论化，需要嵌入具体量化数据",
        "Bullet 太笼统，需要更具体的技术细节",
    ]
    quick_fill = st.radio("快速选择修改意见", ["自定义"] + QUICK_NOTES, key="quick_fill")
    default_note = "" if quick_fill == "自定义" else quick_fill
    revision_notes = st.text_area("修改意见", value=default_note, key="revision_notes_input")

    if st.button("生成修改 Prompt", key="gen_revision_prompt"):
        if revision_notes.strip():
            revision_prompt = build_revision_prompt(
                previous_response=raw_json,
                revision_notes=revision_notes,
                n_candidates=config.candidates_per_element,
            )
            task.revision_count += 1
            task.revision_notes = revision_notes

            runs_dir = resolve_path(config.runs_dir) / task.task_id
            runs_dir.mkdir(parents=True, exist_ok=True)
            old_file = runs_dir / f"ai_response_raw_v{task.revision_count}.txt"
            old_file.write_text(raw_json, encoding="utf-8")

            st.code(revision_prompt, language="markdown")
            if st_copy_to_clipboard:
                st_copy_to_clipboard(revision_prompt, "复制修改 Prompt")
            else:
                st.info("手动选中上方文本复制，粘贴给 AI 后将新结果粘贴回上方。")
        else:
            st.warning("请填写修改意见。")
```

- [ ] **Step 7: Add consistency check before preview generation (Step 5)**

In the Step 5 section, add a consistency check right after the `st.subheader("Step 5:...")` line (before the `candidates = ...` line). Insert:

```python
# --- Step 5: Generate 3 preview variants ---
st.subheader("Step 5: 生成三套预览 → 整体对比选择")

if meta and not check_template_consistency(meta):
    st.warning("⚠ 模板文件在任务进行中已被修改，预览可能不准确。")
    c1, c2 = st.columns(2)
    with c1:
        if st.button("重新预处理", key="repreprocess_step5"):
            st.switch_page("pages/1_template_library.py")
    with c2:
        st.button("忽略继续", key="ignore_consistency_step5")
```

The rest of Step 5 and Step 6 remain unchanged.

- [ ] **Step 8: Verify manually**

Run: `cd C:\Users\lintian\ppt-assistant && python -m streamlit run src/ui/pages/2_new_task.py`
Verify:
1. Style dropdown appears in Step 2, default "huawei"
2. Prompt includes style content when huawei selected
3. Retry section appears after JSON paste with quick-fill options
4. Consistency warning appears if template is modified (test by touching a .pptx file)

- [ ] **Step 9: Commit**

```bash
git add src/ui/pages/2_new_task.py
git commit -m "feat: add style selector, template consistency check, and prompt retry to new task page"
```

---

### Task 8: Integration Test & Final Verification

**Files:**
- All modified files

- [ ] **Step 1: Run full test suite**

Run: `cd C:\Users\lintian\ppt-assistant && python -m pytest tests/ -v`
Expected: All tests pass.

- [ ] **Step 2: Manual smoke test**

Run: `cd C:\Users\lintian\ppt-assistant && python -m streamlit run src/ui/app.py`

Test checklist:
1. Template Library: preprocess a template → verify layout_type dropdown appears in role correction
2. New Task: select template → verify style dropdown (default "huawei") → generate prompt → verify prompt includes style rules and char ranges
3. New Task: paste a dummy JSON → verify retry section appears → click quick-fill → generate revision prompt → verify revision prompt contains previous response and notes
4. New Task: verify consistency warning if template file is touched between steps

- [ ] **Step 3: Final commit with any fixes**

If fixes needed:
```bash
git add -A
git commit -m "fix: integration fixes for phase2 batch1"
```

- [ ] **Step 4: Push to GitHub**

```bash
git push origin master
```

---

## Spec Coverage Check

| Spec Requirement | Task |
|-----------------|------|
| AppConfig.default_style | Task 1 Step 4-5 |
| TaskRun.style_id, revision_count, revision_notes | Task 1 Step 3 |
| ShapeRole.min_chars | Task 1 Step 1 |
| TemplateMeta.layout_type | Task 1 Step 2 |
| estimate_char_capacity() algorithm | Task 2 Step 3 |
| Font size priority (own > median > 12pt) | Task 2 Step 3 (_resolve_font_size) |
| Title single-line cap | Task 2 Step 3 (is_title param) |
| huawei.md style preset | Task 3 Step 2 |
| minimal.md placeholder | Task 3 Step 3 |
| text_generation.md.j2 style/layout blocks | Task 4 Step 1 |
| text_revision.md.j2 | Task 4 Step 2 |
| build_text_prompt style_id param | Task 4 Step 3 |
| build_revision_prompt | Task 4 Step 3 |
| list_available_styles | Task 4 Step 3 |
| check_template_consistency | Task 5 Step 1 |
| layout_type dropdown in template library | Task 6 Step 2 |
| Style dropdown in new task Step 2 | Task 7 Step 3 |
| Consistency check Step 1 | Task 7 Step 2 |
| Consistency check Step 5 | Task 7 Step 7 |
| Retry section Step 4 | Task 7 Step 6 |
| Quick-fill revision notes | Task 7 Step 6 |
| Version old ai_response_raw | Task 7 Step 6 |
